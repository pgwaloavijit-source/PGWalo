# Demo mode: leave empty (uses mockData in browser)
# Production / local Worker API:
# VITE_API_BASE_URL="http://localhost:8787"

# Optional demo-mode Super Admin (used only when the Worker API is unavailable).
# Prefer Worker secrets: SUPERADMIN_PHONE, SUPERADMIN_PIN, SUPERADMIN_USERNAME, SUPERADMIN_PASSWORD
# VITE_SUPERADMIN_PHONE="7070696968"
# VITE_SUPERADMIN_PIN="111111"
# VITE_SUPERADMIN_USERNAME="PGWalo.Avijit"
# VITE_SUPERADMIN_PASSWORD="1q2w3e4r5t"

# Production uses same-origin /api (no env var needed when deployed)
